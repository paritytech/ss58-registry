[package]
name = "ss58-registry-example"
version = "0.0.1"
edition = "2018"
# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[features]
default = ["std"]
std = []

[dependencies]
ss58-registry = { path=".." }
sp-debug-derive = "3.0.0"
